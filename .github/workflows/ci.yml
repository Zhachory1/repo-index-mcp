name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dev package
        run: python -m pip install -e '.[dev]'
      - name: Lint
        run: ruff check .
      - name: Test
        run: pytest -q
      - name: Eval gate
        run: codescry --db .codescry/ci.sqlite eval evals/golden.codescry.jsonl . -k 10 --fail-under 0.85
      - name: NPM package dry-run
        run: npm pack --dry-run
      - name: Build package
        run: python -m build
      - name: Installed console smoke
        run: |
          python -m venv /tmp/codescry-smoke
          /tmp/codescry-smoke/bin/pip install dist/*.whl
          /tmp/codescry-smoke/bin/codescry --help
          /tmp/codescry-smoke/bin/codescry --db /tmp/codescry-smoke/index.sqlite doctor
          mkdir -p /tmp/codescry-fixture
          cd /tmp/codescry-fixture
          git init -b main
          git config user.email test@example.com
          git config user.name Test
          git config commit.gpgsign false
          printf 'def hello_world():\n    return True\n' > app.py
          git add app.py
          git commit -m init
          /tmp/codescry-smoke/bin/codescry --db /tmp/codescry-smoke/index.sqlite index .
          /tmp/codescry-smoke/bin/codescry --db /tmp/codescry-smoke/index.sqlite query hello_world -k 1
